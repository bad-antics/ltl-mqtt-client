// examples/demo.ltl — runnable demo for ltl_mqtt_client

import std::io
use ltl_mqtt_client as lib

fn main() {
    io::println("ltl_mqtt_client demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}