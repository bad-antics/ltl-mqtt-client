// ltl-mqtt-client/main.ltl
//
// MQTT 3.1.1 client — QoS 0/1/2, retained messages, reconnect.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-mqtt-client v0.1.0")
    io::println("MQTT 3.1.1 client — QoS 0/1/2, retained messages, reconnect.")
    0
}
